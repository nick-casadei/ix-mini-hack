import styled from "styled-components";

const NavContainer = styled("nav")`
  background: blue;
`;

const Nav = () => (
  <NavContainer>
    <h1>Nav component</h1>
  </NavContainer>
);

export default Nav;
